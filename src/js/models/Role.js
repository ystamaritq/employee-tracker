// TODO: Write code to define and export the Employee class

class Role {
	constructor(id, tittle, salary, department_id) {
		this.id = id;
		this.tittle = tittle;
		this.salary = salary;
		this.department_id = department_id;
	}
}

module.exports = Role;
